// Generated using "npm run build-templates"

import { meta as meta_nocodb } from "./nocodb/meta";
import { generate as generate_nocodb } from "./nocodb";

const templates = [
  { slug: "nocodb", meta: meta_nocodb, generate: generate_nocodb },
];

export default templates;
